class Solution:
    def findMaxAverage(self, nums: List[int], k: int) -> float:
        
        curr = sum(nums[:k])
        max_ = curr
        
        for i in range(k, len(nums)):
            curr += nums[i] - nums[i - k]
            if curr > max_:
                max_ = curr
                
        return max_ / k