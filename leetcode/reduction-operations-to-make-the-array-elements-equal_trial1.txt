class Solution:
    def reductionOperations(self, nums: List[int]) -> int:
        oper = 0
        n = len(nums)
        nums.sort(reverse=True)
        for i in range(1,n):
            if nums[i] < nums[i-1]:
                oper += i 
        return oper
                
                