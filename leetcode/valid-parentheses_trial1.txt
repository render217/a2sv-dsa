class Solution:
    def isValid(self, s: str) -> bool:
        stack = [] 
        pair = {')':'(','}':'{',']':'['}
        for char in s:
            if char in pair and stack and stack[-1] == pair.get(char):
                stack.pop()
            else:
                stack.append(char)

        return len(stack) == 0